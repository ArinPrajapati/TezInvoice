version: '0.0.1'

services:
  clinet:
    build: client
    ports:
      - 3000:3000
    volumes:
      - ./client:/app
    depends_on:
      - backend
  backend:
    build: server
    ports:
      - 4000:4000
